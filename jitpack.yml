before_install:
  - sdk install java 23.0.2-open
  - sdk use java 23.0.2-open
  - sdk install maven

jdk:
  - openjdk23

install:
  - chmod 777 ./mvnw
  - ./mvnw -s .mvn/settings.xml install -DskipTests